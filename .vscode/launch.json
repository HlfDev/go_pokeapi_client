{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "PokeAPI (Debug)",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "envFile": "${workspaceFolder}/.env",
            "program": "${workspaceFolder}/cmd/main.go",
            "cwd": "${workspaceFolder}"
        }
    ]
}